#ifndef __PRERENDEREVENTS_H__
#define __PRERENDEREVENTS_H__

#include <irrlicht.h>

//needed so we can to render to texture properly
class PrerenderEvents
{
public:
	PrerenderEvents();
	~PrerenderEvents();

protected:

};

#endif
